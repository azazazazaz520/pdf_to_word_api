# AGENTS.md

本文件适用于 `D:\pdf_validation` 项目下的全部工作。代理处理代码、测试、脚本、样本、文档和本地验证时，都应遵守以下约定。

## 项目范围

- 这是一个独立的 PDF 转 Word 项目，代码、测试、样本、文档和运行产物分开管理。
- 开始修改前，先阅读根目录 `README.md`，并按任务需要查看 `docs/project-structure.md` 和相关验证文档。
- 优先理解现有实现和测试，再做范围明确的修改。不要为了当前任务顺手进行无关重构或批量改名。

## 目录约定

- `src/`：核心实现、导出逻辑、PDF 处理和服务入口。
- `tests/`：自动化测试，使用 Python `unittest`。
- `scripts/`：合成 PDF 样本和验证入口脚本。
- `fixtures/`：可复现的合成 PDF 输入。可以按任务需要新增、修改或删除测试样本。
- `docs/`：评估报告、实施记录和验证结论。
- `artifacts/`、`model_cache/`、`service_data/`、`tmp/`：本地运行目录，不属于版本控制内容。

新增文件应放入与其职责匹配的目录。修改既有目录结构时，应同步检查相关导入、测试、脚本和文档。

## Python 环境与命令

- 使用 Python 3.11 和项目虚拟环境，不要依赖系统 Python。
- 在项目根目录的 PowerShell 中运行命令，优先使用：

  ```powershell
  & .\.venv\Scripts\python.exe <参数>
  ```

- 依赖以 `requirements.txt` 为准。除非任务明确要求，不要随意升级或替换 PaddlePaddle、PaddleOCR、PaddleX 及 PDF/DOCX 相关依赖。
- 不要把令牌、密钥、`.env` 文件或真实业务样本写入版本库。

## 验证要求

- 每次代码修改后，默认运行完整的自动化测试集：

  ```powershell
  & .\.venv\Scripts\python.exe -m unittest discover -s tests -v
  ```

- OCR、Word 渲染和服务端端到端验证只在相关代码发生变化时运行。相关代码包括对应的 OCR/worker、布局与导出、渲染校验、服务接口、任务调度和认证模块。
- 修改 `fixtures/` 或验证脚本时，应运行能够覆盖受影响样本和流程的测试；必要时重新生成合成样本。
- 测试失败时先判断是代码回归、测试问题还是本地环境问题。不得静默跳过失败、伪造通过结果或只删除失败产物来掩盖问题。
- 对耗时较长或依赖本地模型的验证，记录实际运行的命令和结果摘要，便于后续复核。

## 本地运行产物

- `artifacts/`、`model_cache/`、`service_data/`、`tmp/`、`outputs/`、`qa/`、`.venv/` 和 Python 缓存目录只允许作为本地运行目录使用。
- 代理不得主动清理、删除或提交这些目录中的内容。测试生成的产物可以留在本地，不应作为代码修改的一部分处理。
- 如果任务明确要求处理某个运行产物，应先确认目标和范围，再进行操作；仍不得把包含密钥、令牌或真实业务数据的内容提交到版本库。

## 代码与注释

- 注释使用中文，表达清晰、易懂，解释代码的含义、约束或稳定的设计原因。
- 避免晦涩、堆砌术语或过度防御性的注释。不要为某一次特定修改留下临时叙事，也不要保留与当前实现不一致的注释。
- 代码结构应优先通过清晰的命名和合理的函数边界表达；只有确实能帮助理解时才增加注释。
- 修改行为时同步更新受影响的测试和必要文档，保持实现、测试和说明一致。

## Git 与提交

- 只有用户明确要求提交时，代理才可以创建 Git commit。
- 提交信息遵循 Conventional Commits，格式为：

  ```text
  <type>(<scope>): <description>
  ```

- 常用类型包括 `feat`、`fix`、`refactor`、`test`、`docs`、`chore`、`perf` 和 `ci`。`scope` 根据受影响的模块填写，例如 `service`、`export`、`layout` 或 `tests`。
- 提交前检查 `git status` 和 `git diff`，只提交当前任务相关的源代码、测试、脚本、文档或样本变更；不要提交本地运行产物、凭据或无关的用户改动。
- 未经用户明确要求，不执行会丢失工作区内容的 Git 操作，例如 `git reset --hard` 或覆盖式 checkout。
